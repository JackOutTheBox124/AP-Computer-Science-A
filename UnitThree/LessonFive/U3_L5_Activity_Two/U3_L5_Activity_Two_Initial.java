package UnitThree.LessonFive.U3_L5_Activity_Two;

import java.util.Scanner;
/**
 * Debug the code in the starter file, so it functions as intended and does not cause any errors. This program is intended to take two integer inputs and determine whether the second is a factor of the first or not.
 * <br>
 * The number b is a factor of the number <code style="color:red">a</code> if <code style="color:red">a</code> can be divided by <code style="color:red">b</code> with no remainder. Remember though that no number can be divided by 0 - so no numbers should be considered a multiple of 0 for the purpose of this exercise.
 */
public class U3_L5_Activity_Two_Initial {
  public static void main(String[] args) {
    // Scanner scan = new Scanner(System.in);
    // System.out.println("Enter two numbers:");
    // int a = scan.nextDouble();
    // int b = scan.nextInt();
    //
    // if((a % b =! 0 & a == 0)
    // System.out.println(b + " is not a factor of " + a);
    // else
    // System.out.println(b + " is a factor of " + a)
  }
}
